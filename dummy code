public int Add(Movie movie)
{
    using (var connection = _dapperContext.CreateConnection())
    {

        var sqlQuery = "\"BookMyShowDB\".spc_insertMovie";
        int res = connection.QuerySingleOrDefault<int>(sqlQuery);
        return res;
    }
   
    
}
